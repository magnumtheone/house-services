name = "house-service-chatbot-proxy"
main = "src/worker.js"
compatibility_date = "2025-01-01"

# La clé API est stockée dans une variable d'environnement secrète.
# Pour la configurer, exécutez :
#   npx wrangler secret put GEMINI_API_KEY
# puis collez votre clé quand demandé.

[vars]
# Domaines autorisés pour la production et tests (séparés par des virgules)
ALLOWED_ORIGINS = "https://houseservice.cd,https://www.houseservice.cd,https://house-service-1omn.onrender.com,http://localhost,http://127.0.0.1,https://localhost,https://127.0.0.1"
